import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['julissa-petechiate-madaline.ngrok-free.dev'],
  reactCompiler: true,
};

export default nextConfig;
